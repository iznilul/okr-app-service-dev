<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.softlab.okr.mapper.SignUpMapper">

    <sql id="baseColumn">

    </sql>
    <select id="queryOK" resultType="int" parameterType="string">
        select status from signup where `name`=#{name}
    </select>

    <update id="switchMenu">
        update signup set status=#{status} where `name`=#{name}
    </update>

    <insert id="addNews" parameterType="">
        insert into signuplist(uid, `name`, gender, qq, className, profile, status, createTime)
        values (#{uid}, #{name}, #{gender}, #{qq}, #{className}, #{profile}, 0, NOW())
    </insert>

    <select id="query" parameterType="string" resultType="int">
        select status from signuplist where uid=#{uid}
    </select>

    <select id="isExist" parameterType="string" resultType="string">
        select uid from signuplist where uid=#{uid} limit 1
    </select>

    <select id="fetchAllStudents" resultType="signuplist">
        select * from signuplist
    </select>
</mapper>
